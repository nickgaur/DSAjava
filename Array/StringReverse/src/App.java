public class App {
    public static void main(String[] args) throws Exception {
        String str = "tim";
        int first = 0;
        int last = str.length() - 1;
        while(first < last){
            char temp = str.charAt(first);
            str[] = str.charAt(last);

        }
    }
}
